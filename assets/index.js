let leftBar = document.getElementById('leftBar');
let rightBar = document.getElementById('rightBar');

// gsap.to( leftBar, .5, { delay: 5, x: '-100%'})
// gsap.to( rightBar, .5, { delay: 5, x: '100%'})